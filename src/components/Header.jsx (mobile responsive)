// components/Header.jsx (Simpler mobile version)
import { useState } from 'react';

export default function Header({ cartCount, onCartOpen }) {
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);

  return (
    <header className="border-b border-gray-200 bg-white/95 backdrop-blur-md sticky top-0 z-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 py-4">
        {/* Top Row */}
        <div className="flex items-center justify-between">
          {/* Left: Hamburger + Logo */}
          <div className="flex items-center space-x-4">
            <button 
              onClick={() => setMobileMenuOpen(!mobileMenuOpen)}
              className="lg:hidden p-2 text-gray-600 hover:bg-gray-100 rounded-full"
            >
              <span className="text-lg">{mobileMenuOpen ? '✕' : '☰'}</span>
            </button>

            <div className="text-xl sm:text-2xl font-serif font-bold bg-gradient-to-r from-gray-900 to-gray-700 bg-clip-text text-transparent">
              REAPERS
            </div>
          </div>

          {/* Desktop Navigation */}
          <nav className="hidden lg:flex items-center space-x-8">
            <button className="text-sm font-medium text-gray-700 hover:text-gray-900 py-2">
              Home
            </button>
            <button className="text-sm font-medium text-gray-700 hover:text-gray-900 py-2">
              Catalog
            </button>
          </nav>

          {/* Right Icons */}
          <div className="flex items-center space-x-3 sm:space-x-4">
            {/* Search - Hidden on mobile when menu is open */}
            {!mobileMenuOpen && (
              <button className="hidden sm:block p-2 text-gray-600 hover:bg-gray-100 rounded-full">
                <span className="text-lg">🔍</span>
              </button>
            )}
            
            <button className="hidden sm:block p-2 text-gray-600 hover:bg-gray-100 rounded-full">
              <span className="text-lg">👤</span>
            </button>

            <button 
              onClick={onCartOpen}
              className="relative p-2 text-gray-600 hover:bg-gray-100 rounded-full"
            >
              <span className="text-lg">🛒</span>
              {cartCount > 0 && (
                <span className="absolute -top-1 -right-1 bg-rose-500 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center">
                  {cartCount}
                </span>
              )}
            </button>
          </div>
        </div>

        {/* Mobile Search Bar - Always visible except when menu is open */}
        {!mobileMenuOpen && (
          <div className="lg:hidden mt-4">
            <div className="flex items-center bg-gray-100 rounded-lg px-4 py-3">
              <span className="text-gray-400 mr-3">🔍</span>
              <input
                type="text"
                placeholder="Search products..."
                className="bg-transparent outline-none text-base w-full"
              />
            </div>
          </div>
        )}
      </div>

      {/* Mobile Menu */}
      {mobileMenuOpen && (
        <div className="lg:hidden bg-white border-t border-gray-200">
          <div className="px-4 py-4 space-y-2">
            <button className="block w-full text-left py-3 px-4 text-base font-medium text-gray-700 hover:bg-gray-50 rounded-lg">
              Home
            </button>
            <button className="block w-full text-left py-3 px-4 text-base font-medium text-gray-700 hover:bg-gray-50 rounded-lg">
              Catalog
            </button>
            <button className="block w-full text-left py-3 px-4 text-base font-medium text-gray-700 hover:bg-gray-50 rounded-lg flex items-center space-x-3">
              <span>👤</span>
              <span>Account</span>
            </button>
          </div>
        </div>
      )}
    </header>
  );
}